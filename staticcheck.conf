# Correctness (SA) and simplification (S1) checks only.
#
# The ST* stylistic checks are deliberately excluded. They would flag this
# codebase's established conventions -- block comments on declarations,
# lowercase error strings prefixed with the module name -- which are
# intentional and consistent, so the findings would be noise rather than
# defects.
checks = ["SA*", "S1*"]
